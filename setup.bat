@echo off

echo Installing required libraries for AppLocker...
pip install psutil schedule


echo If you want to run AppLocker at any directory, you must set to the system environment variable PATH the path to the AppLocker directory.
pause
